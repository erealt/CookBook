<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.estela.cookbook">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_tenedores"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_tenedores"
        android:supportsRtl="true"
        android:theme="@style/Theme.CookBook">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registroUsuarios" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".nuevaReceta" />
        <activity android:name=".actualizarReceta" />
        <activity android:name=".recetasCategorias"/>
        <activity android:name=".detalleReceta" />
    </application>

</manifest>